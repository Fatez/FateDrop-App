const test=require('node:test');const assert=require('node:assert/strict');const{marketSummary}=require('./market-summary');test('market summary aggregates without returning catalogue records',()=>{const result=marketSummary({a:{retailer:'One',availability:'IN_STOCK'},b:{retailer:'Two',availability:'OUT_OF_STOCK'}});assert.equal(result.productsTracked,2);assert.equal(result.inStockCount,1);assert.equal(result.retailerCount,2);assert.equal(result.products,undefined);});
