const test=require('node:test');const assert=require('node:assert/strict');const{hasEntitlement}=require('./entitlements');test('plan features are central and organic discovery remains free',()=>{assert.equal(hasEntitlement('FREE','ORGANIC_DISCOVERY'),true);assert.equal(hasEntitlement('FREE','FEATURED_PLACEMENT'),false);assert.equal(hasEntitlement('INDIE_PRO','RESERVE_AND_COLLECT'),true);});test('Demand Signal depth follows retailer plan',()=>{assert.equal(hasEntitlement('FREE','DEMAND_SIGNAL_BASIC'),false);assert.equal(hasEntitlement('INDIE','DEMAND_SIGNAL_BASIC'),true);assert.equal(hasEntitlement('INDIE','DEMAND_SIGNAL_DETAILED'),false);assert.equal(hasEntitlement('INDIE_PRO','DEMAND_SIGNAL_DETAILED'),true);});
