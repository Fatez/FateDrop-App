const test=require('node:test');const assert=require('node:assert/strict');const{analyticsSummary}=require('./analytics');test('analytics reports only recorded evidence',()=>{const result=analyticsSummary([{retailerId:'one',offerId:'a',createdAt:'2026-01-02T00:00:00Z'}],'one','2026-01-01','2026-01-03');assert.equal(result.outboundClicks,1);assert.equal(result.storefrontViews,null);assert.equal(result.isDemo,false);});
