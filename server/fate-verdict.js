'use strict';

function finite(value){return typeof value==='number'&&Number.isFinite(value);}function positive(value){return finite(value)&&value>0?value:null;}function cleanKey(value){return typeof value==='string'&&value.trim()?value.trim().toLowerCase():null;}
function bestOffer(group){const offers=(Array.isArray(group?.offers)?group.offers:[]).filter((offer)=>finite(offer?.priceGbp));offers.sort((a,b)=>{if(a.priceGbp!==b.priceGbp)return a.priceGbp-b.priceGbp;if(Boolean(a.deliveryKnown)!==Boolean(b.deliveryKnown))return a.deliveryKnown?-1:1;if(a.deliveryKnown&&b.deliveryKnown){const gap=(finite(a.totalDeliveredGbp)?a.totalDeliveredGbp:Infinity)-(finite(b.totalDeliveredGbp)?b.totalDeliveredGbp:Infinity);if(gap!==0)return gap;}const aSeen=Date.parse(a.lastCheckedAt||'')||0,bSeen=Date.parse(b.lastCheckedAt||'')||0;return bSeen-aSeen||String(a.retailerName||'').localeCompare(String(b.retailerName||''));});return offers[0]||null;}
function rrpEvidence(group){const source=typeof group?.rrpSource==='string'&&group.rrpSource.trim()?group.rrpSource:null,kind=typeof group?.rrpKind==='string'&&group.rrpKind.trim()?group.rrpKind:null;if(!source)return null;const directRrpGbp=positive(group?.rrpGbp),unitRrpGbp=positive(group?.unitRrpGbp),unitCount=positive(group?.unitCount),scaledRrpGbp=unitRrpGbp!==null&&unitCount!==null?unitRrpGbp*unitCount:null;if(directRrpGbp!==null&&scaledRrpGbp!==null&&Math.abs(directRrpGbp-scaledRrpGbp)>0.005)return null;const rrpGbp=directRrpGbp??scaledRrpGbp;if(rrpGbp===null)return null;return{rrpGbp,directRrpGbp,unitRrpGbp,unitCount,unitKind:cleanKey(group?.unitKind),source,kind,observedAt:typeof group?.rrpObservedAt==='string'?group.rrpObservedAt:null,basis:typeof group?.rrpReferenceBasis==='string'?group.rrpReferenceBasis:null,scaledFromUnit:directRrpGbp===null&&scaledRrpGbp!==null};}
function effectiveRrp(group){return rrpEvidence(group)?.rrpGbp??null;}function sameComparableFamily(leftGroup,rightGroup){const li=cleanKey(leftGroup?.identityKey),ri=cleanKey(rightGroup?.identityKey);if(li&&ri&&li===ri)return true;const lf=cleanKey(leftGroup?.valueFamilyKey),rf=cleanKey(rightGroup?.valueFamilyKey);return Boolean(lf&&rf&&lf===rf);}
function valuePosition(group){const offer=bestOffer(group);if(!offer)return null;const itemPrice=finite(offer.priceGbp)?offer.priceGbp:null,truePrice=offer.deliveryKnown&&finite(offer.totalDeliveredGbp)?offer.totalDeliveredGbp:null,reference=rrpEvidence(group),rrpGbp=reference?.rrpGbp??null,rrpPercent=itemPrice!==null&&rrpGbp!==null?((itemPrice-rrpGbp)/rrpGbp)*100:null,unitCount=positive(group?.unitCount),unitCost=truePrice!==null&&unitCount!==null?truePrice/unitCount:null;return{groupId:group.id,title:group.title,identityKey:cleanKey(group?.identityKey),valueFamilyKey:cleanKey(group?.valueFamilyKey),offerId:offer.id,retailerId:offer.retailerId,retailerName:offer.retailerName,itemPrice,truePrice,checkoutCost:truePrice,rrpGbp,rrpPercent,unitCount,unitKind:cleanKey(group?.unitKind),unitCost,deliveryKnown:Boolean(offer.deliveryKnown),provisional:!offer.deliveryKnown,reference,truePriceEvidence:{itemPriceGbp:itemPrice,deliveryGbp:offer.deliveryKnown&&finite(offer.shippingGbp)?offer.shippingGbp:null,totalGbp:truePrice,deliveryKnown:Boolean(offer.deliveryKnown),retailerName:offer.retailerName||null,observedAt:offer.lastCheckedAt||null,stockStatus:offer.stockStatus||null}};}
function noWinner(left,right,reason){return{left,right,winnerId:null,basis:null,gap:null,reason};}
function compareGroups(leftGroup,rightGroup){const left=valuePosition(leftGroup),right=valuePosition(rightGroup);if(!left||!right||left.groupId===right.groupId)return noWinner(left,right,'Choose two different comparable items.');if(!sameComparableFamily(leftGroup,rightGroup))return noWinner(left,right,'FateDrop could not verify that these items are equivalent members of the same value family.');const lh=left.rrpPercent!==null,rh=right.rrpPercent!==null;if(lh!==rh)return noWinner(left,right,'FateDrop needs verified RRP/reference evidence for both items before declaring an RRP-based winner.');if(lh&&rh){const gap=Math.abs(left.rrpPercent-right.rrpPercent);if(gap>1e-9){const winner=left.rrpPercent<right.rrpPercent?left:right;return{left,right,winnerId:winner.groupId,basis:'rrp_percent',gap,reason:`${winner.title} has the better value position versus its verified RRP/reference baseline based on item price.`};}if(left.truePrice!==null&&right.truePrice!==null&&Math.abs(left.truePrice-right.truePrice)>1e-9){const winner=left.truePrice<right.truePrice?left:right;return{left,right,winnerId:winner.groupId,basis:'rrp_percent',gap:0,reason:`${winner.title} matches the RRP value position and has the lower known True Price.`};}return noWinner(left,right,'These items currently have the same verified RRP value position and no trustworthy known True Price tie-break.');}const safe=left.unitCost!==null&&right.unitCost!==null&&left.unitKind&&left.unitKind===right.unitKind;if(safe){const gap=Math.abs(left.unitCost-right.unitCost);if(gap<=1e-9)return noWinner(left,right,'These items currently have the same known True Price per comparable unit.');const winner=left.unitCost<right.unitCost?left:right;return{left,right,winnerId:winner.groupId,basis:'unit_true_price',gap,reason:`${winner.title} has the lower known True Price per ${winner.unitKind==='booster_pack'?'pack':'unit'}.`};}return noWinner(left,right,'FateDrop needs comparable verified RRP/reference or unit evidence before declaring a winner.');}
function rankGroups(groups){const source=Array.isArray(groups)?groups:[],positioned=source.map((group)=>({group,position:valuePosition(group)})).filter((entry)=>entry.position),positions=positioned.map((entry)=>entry.position),provisional=positions.some((item)=>item.provisional);if(!positions.length)return{winnerId:null,basis:null,reason:'No purchasable offers are available to rank.',provisional,ranking:[]};if(positioned.length>1&&positioned.some((entry)=>!sameComparableFamily(positioned[0].group,entry.group)))return{winnerId:null,basis:null,reason:'FateDrop found mixed or unverified product identities, so it will not declare one cross-product winner.',provisional,ranking:positions};const all=positions.every((item)=>item.rrpPercent!==null),none=positions.every((item)=>item.rrpPercent===null);if(!all&&!none)return{winnerId:null,basis:null,reason:'FateDrop needs verified RRP/reference evidence for every comparable candidate before declaring a winner.',provisional,ranking:positions};if(all){const ranking=[...positions].sort((a,b)=>{const gap=a.rrpPercent-b.rrpPercent;if(Math.abs(gap)>1e-9)return gap;if(a.truePrice!==null&&b.truePrice!==null&&a.truePrice!==b.truePrice)return a.truePrice-b.truePrice;if(a.deliveryKnown!==b.deliveryKnown)return a.deliveryKnown?-1:1;return 0;}),winner=ranking[0],runnerUp=ranking[1],tied=Boolean(runnerUp&&Math.abs(winner.rrpPercent-runnerUp.rrpPercent)<=1e-9&&(winner.truePrice===null||runnerUp.truePrice===null||Math.abs(winner.truePrice-runnerUp.truePrice)<=1e-9));return{winnerId:tied?null:winner.groupId,basis:'rrp_percent',reason:tied?'The leading candidates are tied on verified RRP value position and available True Price evidence.':`${winner.title} has the strongest value position versus its verified RRP/reference baseline across the comparable searched items.`,provisional,ranking};}const unitKinds=new Set(positions.map((item)=>item.unitKind).filter(Boolean)),comparable=positions.every((item)=>item.unitCost!==null&&item.unitKind)&&unitKinds.size===1;if(comparable){const ranking=[...positions].sort((a,b)=>a.unitCost-b.unitCost),winner=ranking[0],tied=ranking[1]&&Math.abs(winner.unitCost-ranking[1].unitCost)<=1e-9;return{winnerId:tied?null:winner.groupId,basis:'unit_true_price',reason:tied?'The leading candidates are tied on known True Price per comparable unit.':`${winner.title} has the lowest known True Price per ${winner.unitKind==='booster_pack'?'pack':'unit'} across the comparable searched items.`,provisional,ranking};}return{winnerId:null,basis:null,reason:'FateDrop cannot declare a trustworthy best deal until the searched items have comparable RRP/reference or unit evidence.',provisional,ranking:positions};}
module.exports={bestOffer,effectiveRrp,rrpEvidence,sameComparableFamily,valuePosition,compareGroups,rankGroups};