const HOUR=3600000,DAY=24*HOUR;
function dropPulse(product,events=[],now=Date.now()){const labels=[];const first=Date.parse(product.firstSeen),restock=Date.parse(product.returnedAt),checked=Date.parse(product.lastSeen);if(Number.isFinite(first)&&now-first<=24*HOUR)labels.push('JUST_LISTED');if(Number.isFinite(restock)&&now-restock<=12*HOUR)labels.push('RECENTLY_RESTOCKED');if((product.timesRestocked||0)>=3)labels.push('RESTOCKED_REPEATEDLY');const priceDrop=events.find(event=>event.type==='PRICE_CHANGE'&&Number(event.previousValues?.price)>Number(product.price));if(priceDrop)labels.push('PRICE_DROPPED');const clicks=events.filter(event=>event.type==='OUTBOUND_CLICK'&&now-Date.parse(event.detectedAt)<=DAY).length;if(clicks>=25)labels.push('HIGH_INTEREST');if(Number.isFinite(checked)&&now-checked>6*HOUR)labels.push('STOCK_MAY_BE_STALE');return labels;}
module.exports={dropPulse};
