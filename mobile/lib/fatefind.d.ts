import type { ProductOffer,SavedSearch } from '@/types/domain';
export function matchesFateFind(search:SavedSearch,offer:ProductOffer):boolean;
export function fateFindSummary(search:SavedSearch):string;
